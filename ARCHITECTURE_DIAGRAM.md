# Architecture Diagram

## Current Setup: Flexible Backend Configuration

Your Jharkhand Tourism application now supports two deployment architectures:

---

## Option 1: Monolithic Deployment (Same Server)

```
┌─────────────────────────────────────────────────┐
│                                                 │
│         Render / Vercel Deployment              │
│                                                 │
│  ┌───────────────────────────────────────────┐ │
│  │                                           │ │
│  │         Next.js Application               │ │
│  │                                           │ │
│  │  ┌─────────────┐    ┌─────────────────┐  │ │
│  │  │             │    │                 │  │ │
│  │  │  Frontend   │◄───┤   API Routes    │  │ │
│  │  │  (React)    │    │   /api/*        │  │ │
│  │  │             │    │                 │  │ │
│  │  └─────────────┘    └────────┬────────┘  │ │
│  │                              │           │ │
│  └──────────────────────────────┼───────────┘ │
│                                 │             │
└─────────────────────────────────┼─────────────┘
                                  │
                                  ▼
                    ┌──────────────────────────┐
                    │                          │
                    │   PostgreSQL Database    │
                    │   (Render)               │
                    │                          │
                    └──────────────────────────┘

Configuration:
  NEXT_PUBLIC_API_URL=  (empty or not set)
```

**Pros:**
- ✅ Simple deployment
- ✅ No CORS issues
- ✅ Single URL
- ✅ Easier to manage

**Cons:**
- ❌ Slower cold starts (Render free tier)
- ❌ Can't scale frontend/backend independently

---

## Option 2: Separated Deployment (Microservices)

```
┌─────────────────────────────────┐
│                                 │
│   Vercel / Netlify              │
│   (Frontend Only)               │
│                                 │
│  ┌───────────────────────────┐  │
│  │                           │  │
│  │   Next.js Frontend        │  │
│  │   (Static + Client)       │  │
│  │                           │  │
│  │   ┌─────────────────┐     │  │
│  │   │                 │     │  │
│  │   │  React Pages    │     │  │
│  │   │  Components     │     │  │
│  │   │                 │     │  │
│  │   └────────┬────────┘     │  │
│  │            │              │  │
│  └────────────┼──────────────┘  │
│               │                 │
└───────────────┼─────────────────┘
                │
                │ HTTPS
                │ (CORS Enabled)
                │
                ▼
┌───────────────────────────────────────────┐
│                                           │
│   Render (Backend Only)                   │
│   https://jharkhand-tourism-li3w...       │
│                                           │
│  ┌─────────────────────────────────────┐  │
│  │                                     │  │
│  │   Next.js API Routes                │  │
│  │   /api/*                            │  │
│  │                                     │  │
│  │   ┌──────────────────────────────┐  │  │
│  │   │                              │  │  │
│  │   │  Authentication              │  │  │
│  │   │  Bookings                    │  │  │
│  │   │  Admin Operations            │  │  │
│  │   │  Email Notifications         │  │  │
│  │   │  Payment Processing          │  │  │
│  │   │                              │  │  │
│  │   └──────────────┬───────────────┘  │  │
│  │                  │                  │  │
│  └──────────────────┼──────────────────┘  │
│                     │                     │
└─────────────────────┼─────────────────────┘
                      │
                      ▼
        ┌──────────────────────────┐
        │                          │
        │   PostgreSQL Database    │
        │   (Render)               │
        │                          │
        └──────────────────────────┘

Configuration:
  Frontend: NEXT_PUBLIC_API_URL=https://jharkhand-tourism-li3w.onrender.com
  Backend:  NEXTAUTH_URL=https://jharkhand-tourism-li3w.onrender.com
```

**Pros:**
- ✅ Fast frontend (Vercel Edge Network)
- ✅ Independent scaling
- ✅ Better performance
- ✅ Can serve multiple frontends (web, mobile)

**Cons:**
- ❌ Requires CORS configuration
- ❌ More complex setup
- ❌ Two deployments to manage

---

## Request Flow (Separated Architecture)

```
User Browser
     │
     │ 1. Visit website
     ▼
┌─────────────────┐
│  Vercel CDN     │  ◄── Fast global delivery
│  (Frontend)     │
└────────┬────────┘
         │
         │ 2. User action (login, book, etc.)
         │
         ▼
┌─────────────────────────────────────┐
│  lib/config.ts                      │
│  getApiUrl('/api/auth/login')       │
│  ↓                                  │
│  Returns: https://jharkhand-        │
│  tourism-li3w.onrender.com/         │
│  api/auth/login                     │
└────────┬────────────────────────────┘
         │
         │ 3. API call with CORS headers
         │
         ▼
┌─────────────────────────────────────┐
│  middleware.ts (Backend)            │
│  - Check origin                     │
│  - Add CORS headers                 │
│  - Allow credentials                │
└────────┬────────────────────────────┘
         │
         │ 4. Process request
         │
         ▼
┌─────────────────────────────────────┐
│  API Route Handler                  │
│  - Authenticate user                │
│  - Query database                   │
│  - Send email                       │
│  - Process payment                  │
└────────┬────────────────────────────┘
         │
         │ 5. Database query
         │
         ▼
┌─────────────────────────────────────┐
│  PostgreSQL (Prisma)                │
│  - Users, Hotels, Bookings, etc.    │
└────────┬────────────────────────────┘
         │
         │ 6. Return data
         │
         ▼
┌─────────────────────────────────────┐
│  Response with CORS headers         │
│  - Access-Control-Allow-Origin      │
│  - Access-Control-Allow-Credentials │
└────────┬────────────────────────────┘
         │
         │ 7. Receive response
         │
         ▼
┌─────────────────────────────────────┐
│  useApi Hook                        │
│  - Update loading state             │
│  - Handle success/error             │
│  - Update UI                        │
└─────────────────────────────────────┘
         │
         │ 8. Display result
         │
         ▼
    User Browser
```

---

## Configuration System

```
┌──────────────────────────────────────────────┐
│  Environment Variables                       │
│                                              │
│  NEXT_PUBLIC_API_URL=                        │
│  https://jharkhand-tourism-li3w.onrender.com │
│                                              │
└──────────────────┬───────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────┐
│  lib/config.ts                               │
│                                              │
│  export const API_CONFIG = {                 │
│    baseURL: process.env.NEXT_PUBLIC_API_URL  │
│              || '',                          │
│    timeout: 30000,                           │
│    withCredentials: true                     │
│  }                                           │
│                                              │
│  export function getApiUrl(endpoint) {       │
│    if (API_CONFIG.baseURL) {                 │
│      return `${API_CONFIG.baseURL}${endpoint}`│
│    }                                         │
│    return endpoint  // relative path         │
│  }                                           │
└──────────────────┬───────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────┐
│  lib/hooks/useApi.ts                         │
│                                              │
│  export function useApi() {                  │
│    const request = async (endpoint, opts) => {│
│      const url = getApiUrl(endpoint)         │
│      const response = await fetch(url, opts) │
│      return response.json()                  │
│    }                                         │
│    return { request, get, post, put, delete }│
│  }                                           │
└──────────────────┬───────────────────────────┘
                   │
                   ▼
┌──────────────────────────────────────────────┐
│  Your Components                             │
│                                              │
│  const { post } = useApi()                   │
│  await post('/api/auth/login', data)         │
│                                              │
│  // Automatically uses correct backend URL   │
└──────────────────────────────────────────────┘
```

---

## CORS Flow

```
Frontend (Vercel)                Backend (Render)
     │                                │
     │  1. OPTIONS /api/auth/login    │
     ├───────────────────────────────►│
     │     Origin: https://...        │
     │                                │
     │                           ┌────▼────┐
     │                           │middleware│
     │                           │.ts      │
     │                           └────┬────┘
     │                                │
     │                           Check origin
     │                           in allowedOrigins
     │                                │
     │  2. 200 OK                     │
     │◄───────────────────────────────┤
     │  Access-Control-Allow-Origin   │
     │  Access-Control-Allow-Methods  │
     │  Access-Control-Allow-Headers  │
     │                                │
     │  3. POST /api/auth/login       │
     ├───────────────────────────────►│
     │     { email, password }        │
     │                                │
     │                           ┌────▼────┐
     │                           │ API     │
     │                           │ Handler │
     │                           └────┬────┘
     │                                │
     │  4. 200 OK                     │
     │◄───────────────────────────────┤
     │  { token, user }               │
     │  + CORS headers                │
     │                                │
     ▼                                ▼
```

---

## Database Schema

```
┌─────────────────────────────────────────────┐
│  PostgreSQL Database (Render)               │
│                                             │
│  ┌─────────┐  ┌─────────┐  ┌──────────┐    │
│  │  Users  │  │ Hotels  │  │  Places  │    │
│  ├─────────┤  ├─────────┤  ├──────────┤    │
│  │ id      │  │ id      │  │ id       │    │
│  │ email   │  │ name    │  │ name     │    │
│  │ role    │  │ location│  │ category │    │
│  └────┬────┘  └────┬────┘  └──────────┘    │
│       │            │                        │
│       │            │                        │
│  ┌────▼────────────▼────┐  ┌──────────┐    │
│  │     Bookings        │  │  Events  │    │
│  ├─────────────────────┤  ├──────────┤    │
│  │ id                  │  │ id       │    │
│  │ userId (FK)         │  │ name     │    │
│  │ hotelId (FK)        │  │ date     │    │
│  │ checkIn             │  └──────────┘    │
│  │ checkOut            │                  │
│  │ status              │  ┌──────────┐    │
│  └─────────────────────┘  │   OTP    │    │
│                           ├──────────┤    │
│  ┌─────────────────────┐  │ email    │    │
│  │    Restaurants      │  │ code     │    │
│  ├─────────────────────┤  │ expires  │    │
│  │ id                  │  └──────────┘    │
│  │ name                │                  │
│  │ cuisine             │                  │
│  └─────────────────────┘                  │
│                                           │
└───────────────────────────────────────────┘
```

---

## Deployment Workflow

```
Developer
    │
    │ git push
    ▼
┌─────────────┐
│   GitHub    │
│  Repository │
└──────┬──────┘
       │
       ├──────────────────┬──────────────────┐
       │                  │                  │
       ▼                  ▼                  ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   Vercel     │  │    Render    │  │   Render     │
│  (Frontend)  │  │  (Backend)   │  │ (Database)   │
├──────────────┤  ├──────────────┤  ├──────────────┤
│ Auto-deploy  │  │ Auto-deploy  │  │ Always on    │
│ on push      │  │ on push      │  │              │
│              │  │              │  │              │
│ Build:       │  │ Build:       │  │ PostgreSQL   │
│ next build   │  │ prisma gen   │  │ 256MB        │
│              │  │ next build   │  │              │
│              │  │              │  │              │
│ Deploy to    │  │ Start:       │  │              │
│ Edge Network │  │ next start   │  │              │
└──────────────┘  └──────────────┘  └──────────────┘
```

---

## Summary

Your application now supports:

✅ **Flexible Architecture** - Choose monolithic or microservices
✅ **Automatic URL Handling** - Configuration system handles backend URLs
✅ **CORS Support** - Middleware enables cross-origin requests
✅ **Easy Migration** - Minimal code changes needed
✅ **Production Ready** - Configured for Render and Vercel

**Current Setup:**
- Backend: https://jharkhand-tourism-li3w.onrender.com
- Frontend: Can be deployed anywhere
- Database: Render PostgreSQL

**Configuration:**
```env
NEXT_PUBLIC_API_URL=https://jharkhand-tourism-li3w.onrender.com
```

This setup gives you the flexibility to deploy however you want! 🚀
